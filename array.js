// Learning basics to arrays in js


// this is how to reverse an array
function reverseArray(arr){
    arr.reverse();

    // You can manaually do it like this
    // let result = [];
    // for(let i = arr.length - 1; i >= 0; i--) result.push(arr.[i]);
    // return result
}

